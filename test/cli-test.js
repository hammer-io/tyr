describe('Test CLI Functionality', () => {

  describe('Sign in to GitHub', () => {
    it('should sign in to GitHub and return the credentials', () => {

    });

    it('should reprompt the user if the credentials are invalid', () =>  {

    });
  });

  describe('Sign in to Heroku', () => {
    it('should sign in to Heroku and return the credentials', () => {

    });

    it('should reprompt the user if the credentials are invalid', () =>  {

    });
  });
});